repos:
  - repo: local
    hooks:
      - id: markdownlint
        name: markdownlint
        entry: markdownlint
        language: system
        types: [markdown]
        args: [
          --config=.markdownlint.json, 
          --ignore-path=.markdownlintignore
          ]